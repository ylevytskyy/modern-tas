<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<scenario name="S-3 caller hangup mid-screen-pop">

  <send retrans="500">
    <![CDATA[
      INVITE sip:+15555550100@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp" <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:+15555550100@[remote_ip]:[remote_port]>
      Call-ID: [callid]
      CSeq: 1 INVITE
      Contact: <sip:sipp@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[local_ip_type] [local_ip]
      t=0 0
      m=audio 8000 RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <!-- Asterisk sends 100 Trying immediately; recv it before pausing. -->
  <recv response="100" />

  <!-- BYE fires during the screen-pop window (S-3): 1500 ms gives Playwright
       enough time to see the screen-pop render before the caller hangs up,
       but fires well before the operator can accept (3000 ms budget). -->
  <pause milliseconds="1500" />

  <send retrans="500">
    <![CDATA[
      BYE sip:+15555550100@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp" <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:+15555550100@[remote_ip]:[remote_port]>
      Call-ID: [callid]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="481" optional="true" />

</scenario>
