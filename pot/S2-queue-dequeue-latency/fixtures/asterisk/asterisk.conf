; S2 PoT — minimal asterisk.conf. Directory layout is left to the package
; defaults (Ubuntu 24.04 asterisk uses /usr/lib/asterisk/modules,
; /var/lib/asterisk, /var/log/asterisk etc. compiled in).

[options]
verbose   = 3
nocolor   = yes
runuser   = root
rungroup  = root
